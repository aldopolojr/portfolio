export default {
    settings(state) {
        return state.settings;
    },
    modes(state) {
        return state.modes;
    },
    display(state) {
        return state.display;
    },
    apps(state) {
        return state.appModes;
    },
}